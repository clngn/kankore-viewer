{
  "name": "kankore",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "start": "electron src/main.js",
    "asar": "asar pack . kankore.asar  --unpack-dir 'packages'",
    "package": "electron-packager . kankore --out=packages --platform=darwin --arch=x64 --ignore=packages/*  --asar --overwrite"
  },
  "author": "clngn",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^7.0.3",
    "electron-prebuilt": "^1.1.1"
  }
}
